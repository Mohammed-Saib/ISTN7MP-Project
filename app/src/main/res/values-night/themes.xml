<resources>

    <!-- Dark theme — deep navy background, bright blue primary -->
    <style name="Theme.MPProject" parent="Theme.Material3.Dark.NoActionBar">
        <item name="colorPrimary">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/on_bg_dark</item>
        <item name="colorPrimaryContainer">@color/primary_container_dark</item>
        <item name="colorSurface">@color/surface_dark</item>
        <item name="colorSurfaceVariant">@color/surface_raised_dark</item>
        <item name="android:colorBackground">@color/bg_dark</item>
        <item name="colorOnBackground">@color/on_bg_dark</item>
        <item name="colorOnSurface">@color/on_bg_dark</item>
        <item name="colorOutline">@color/outline_dark</item>
        <item name="colorError">@color/error</item>
        <item name="colorOnError">@color/on_error</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@color/surface_dark</item>
        <item name="bottomNavigationStyle">@style/Widget.MPProject.BottomNavigationView.Night</item>
    </style>

    <style name="Widget.MPProject.BottomNavigationView.Night" parent="Widget.Material3.BottomNavigationView">
        <item name="android:background">@color/surface_dark</item>
        <item name="itemIconTint">@color/bottom_nav_item_color</item>
        <item name="itemTextColor">@color/bottom_nav_item_color</item>
        <item name="labelVisibilityMode">labeled</item>
    </style>

</resources>

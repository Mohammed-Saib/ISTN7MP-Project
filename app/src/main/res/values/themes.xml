<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Light theme — warm off-white background, blue primary -->
    <style name="Theme.MPProject" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/primary_light</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        <item name="colorPrimaryContainer">@color/primary_container_light</item>
        <item name="colorSurface">@color/surface_light</item>
        <item name="colorSurfaceVariant">@color/surface_raised_light</item>
        <item name="android:colorBackground">@color/bg_light</item>
        <item name="colorOnBackground">@color/on_bg_light</item>
        <item name="colorOnSurface">@color/on_bg_light</item>
        <item name="colorOutline">@color/outline_light</item>
        <item name="colorError">@color/error</item>
        <item name="colorOnError">@color/on_error</item>
        <!-- Status and navigation bars blend into the app background -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@color/surface_light</item>
        <item name="android:windowLightStatusBar" tools:targetApi="23">true</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="27">true</item>
        <!-- Bottom navigation styling -->
        <item name="bottomNavigationStyle">@style/Widget.MPProject.BottomNavigationView</item>
    </style>

    <!-- Pill-shaped FAB (used by ExtendedFloatingActionButton across all screens) -->
    <style name="ShapeAppearance.MPProject.FAB" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

    <!-- Calendar day-of-week header cell -->
    <style name="CalDayHeader">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">32dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">12sp</item>
        <item name="android:textColor">?attr/colorOnSurface</item>
        <item name="android:alpha">0.6</item>
    </style>

    <!-- Bottom nav: pill indicator, no label shifting -->
    <style name="Widget.MPProject.BottomNavigationView" parent="Widget.Material3.BottomNavigationView">
        <item name="android:background">@color/surface_light</item>
        <item name="itemIconTint">@color/bottom_nav_item_color</item>
        <item name="itemTextColor">@color/bottom_nav_item_color</item>
        <item name="labelVisibilityMode">labeled</item>
    </style>

</resources>
